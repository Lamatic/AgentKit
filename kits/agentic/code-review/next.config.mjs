/** @type {import('next').NextConfig} */
const nextConfig = {
const nextConfig = {
  typescript: {
    ignoreBuildErrors: false,
  },
}
  images: {
    unoptimized: true,
  },
}

export default nextConfig
