Approved requirements JSON:

{{triggerNode_1.output.requirements}}

Draft a minimal set of implementation task proposals. Return title, description, priority, complexity, required skills, dependencies, acceptance criteria, and linked requirement IDs.
