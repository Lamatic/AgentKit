import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'Meeting Action Items Agent | Lamatic AgentKit',
  description: 'Paste raw meeting notes and instantly extract prioritized action items, key decisions, and a follow-up email draft — powered by Lamatic AI.',
  keywords: ['meeting', 'action items', 'AI', 'productivity', 'automation', 'lamatic'],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`font-sans antialiased`} suppressHydrationWarning>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
