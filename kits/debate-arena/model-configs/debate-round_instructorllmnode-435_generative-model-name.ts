// Model config: Generate JSON (InstructorLLMNode_435)
// Flow: debate-round
// Provider: groq-llm (Llama 3.3) — select your own credential in Studio after import

export default {
  generativeModelName: "",
};
