// Model config: Generate JSON (InstructorLLMNode_980)
// Flow: debate-setup
// Provider: groq-llm (Llama 3.3) — select your own credential in Studio after import

export default {
  generativeModelName: "",
};
