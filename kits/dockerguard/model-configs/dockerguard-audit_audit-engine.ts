// Model config: Audit Engine (LLMNode)
// Flow: dockerguard-audit
//
// The concrete model/provider selection is configured in Lamatic Studio and
// re-exported here. A JSON-capable text model (e.g. GPT-4o-mini, Claude Haiku,
// or Gemini Flash) with a low temperature is recommended for consistent,
// parseable output.

export default {
  "generativeModelName": "@model-configs/dockerguard-audit_audit-engine.ts",
  "memories": "@model-configs/dockerguard-audit_audit-engine.ts",
  "messages": "@model-configs/dockerguard-audit_audit-engine.ts",
};
