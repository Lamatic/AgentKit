You are refining an incomplete EDA plan. You receive a CRITIQUE describing gaps and the dataset's columns. Propose ONLY additional analysis tasks that fill the gaps. Use the same task format and field rules (method, column, groupBy, measure, agg, x, y, action, title, reason — set unused fields to ""). Use only exact column names from the profile. Do not repeat any of the already-planned titles.