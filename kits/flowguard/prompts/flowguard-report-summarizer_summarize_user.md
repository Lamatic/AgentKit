Write the executive summary for this evaluation run.

## Regression verdict (or "NONE" if this is a first run with no baseline)
{{triggerNode_1.output.verdict}}

## Run totals (JSON)
{{triggerNode_1.output.totals}}

## Worst failures (JSON array — case id, category, expectedBehavior, actualOutput excerpt, judge rationale)
{{triggerNode_1.output.worstFailures}}

## Baseline deltas (JSON — per-case score changes, flipped cases; empty if no baseline)
{{triggerNode_1.output.baselineDeltas}}

Return Markdown following the required structure. Lead with the verdict headline.
