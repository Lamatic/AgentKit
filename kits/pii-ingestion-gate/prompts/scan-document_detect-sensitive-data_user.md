Scan the following document for PII, credentials, financial, health, and confidential data before it is ingested into a RAG vector index.

Ingestion policy (may be empty — if empty, apply the default severity rubric):
{{triggerNode_1.output.policy}}

Document to scan:
"""
{{triggerNode_1.output.document}}
"""
