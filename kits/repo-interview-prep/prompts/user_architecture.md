Analyze the following repository markdown. Return JSON with:
1. `mermaid_diagram`: A valid mermaid.js flowchart showing the architecture. If impossible, return an empty string. Do not wrap in ```mermaid backticks, just the raw code.
2. `flow_summary`: A textual summary of data flow.
3. `tradeoffs`: Array of strings highlighting design trade-offs made.

Repository:
{{firecrawlNode_808.output.markdown}}
