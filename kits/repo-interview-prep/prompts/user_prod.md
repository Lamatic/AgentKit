Analyze the following repository markdown. Return JSON with:
1. `is_production_ready`: boolean.
2. `critical_missing_features`: Array of strings (e.g. missing CI/CD, bad auth, missing error handling).
3. `quick_wins`: Array of strings to quickly fix these.

Repository:
{{firecrawlNode_808.output.markdown}}
