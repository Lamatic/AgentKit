# Search Widget

This flow builds a search widget that retrieves data from a vector database and a RAG Node, presenting the information in the ideal widget format.

## Type
template

## What This Does
This flow builds a search widget that retrieves data from a vector database and a RAG Node, presenting the information in the ideal widget format.

## Flows
- **search-widget**: This flow builds a search widget that retrieves data from a vector database and a RAG Node, presenting the information in the ideal widget format.

## Author
Naitik Kapadia (naitikk@lamatic.ai)
