# Provider credentials used by the deployed flow's LLM node.
# These are configured in Lamatic Studio under Settings -> Providers, not
# read directly by this repo — kept here for local reference only.
GROQ_API_KEY=your_groq_api_key
