Week to plan starts on: {{week_start_date}}
Output language: {{language}}

Categories:
```json
{{categories}}
```

Fixed commitments:
```json
{{fixed_commitments}}
```

Recurring goals:
```json
{{recurring_goals}}
```

One-off events this week:
```json
{{oneoff_events}}
```

Preferences:
```json
{{preferences}}
```

Generate the week now, following the placement order in the system prompt. Emit `GenerateWeekOutput`.
