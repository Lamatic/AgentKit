Output language: {{language}}

Current blocks (the full week):
```json
{{current_blocks}}
```

Categories:
```json
{{categories}}
```

Recurring goals (for `unmet_goals` recompute):
```json
{{recurring_goals}}
```

Fixed commitments (for validation):
```json
{{fixed_commitments}}
```

Preferences:
```json
{{preferences}}
```

Change event:
```json
{{change}}
```

Apply the change following the rules in the system prompt. Emit `ReplanOutput`.
